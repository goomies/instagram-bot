{% extends 'bot/layout.html.twig' %}

{% block content %}
    {# {% for entry in blog_entries %}
        <h2>{{ entry.title }}</h2>
        <p>{{ entry.body }}</p>
    {% endfor %} #}
    CONTENT
{% endblock %}